# このリポジトリの規約

<!--
  置き場所: あなたのリポジトリの .agent/conventions.md

  この文書は全エージェント（planner / plan-reviewer / developer / dev-reviewer /
  completion）のプロンプトに、そのまま差し込まれます。パイプラインはあなたのリポジトリの
  流儀を知らないので、守らせたいことはここに書きます。

  - **埋めなかった節は削ってください。** 見出しだけが残ると、エージェントは「規約が無い」
    ではなく「空の規約がある」と読みます
  - 書くのは**このリポジトリで判断が分かれること**だけにしてください。一般的な良い作法
    （読みやすく書く、テストを書く）は既定のプロンプトに入っています
  - 長くなるほど 1 実行あたりの費用が増えます。目安は 100 行以内です
-->

## 1. 技術スタックと制約

- 言語 / フレームワーク / バージョン:
- 触ってはいけない領域:
  - `.github/workflows/**`（エージェントの起動条件。差分に含まれると `blocked` になります）
  - `.agent/**`（エージェントに課された設定そのもの。承認者・上限・規約・実行前の準備。
    同じく差分に含まれると `blocked` になります）
  - `.claude/**`（Claude Code がセンシティブファイルとして書き込みを拒否します。
    変更が必要な場合は、完成品をリポジトリに置いて人間が設置します）
  - （ここにあなたのリポジトリの分を足してください。例: マイグレーション、公開 API の互換性、
    自動生成ファイル）

## 2. コーディング規約

- lint / formatter とその実行方法:
- 命名、ディレクトリ配置の慣習:

## 3. テスト

- テストの実行コマンド:
- 新規コードに求めるテストの種類と置き場所:

## 4. PR の作法

- コミットメッセージの形式:
- PR 本文に必須の項目:

## 5. 判断に迷ったとき

- 優先順位（例: 互換性 > 性能 > 簡潔さ）:
- 勝手に決めず、計画の「前提」に書き出してほしいこと:
