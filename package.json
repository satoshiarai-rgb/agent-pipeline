{
  "name": "agent-pipeline",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "format": "biome check --write",
    "lint": "biome check"
  },
  "dependencies": {
    "@actions/core": "^1.11.0",
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.12",
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  }
}
